import 'package:flutter/material.dart';
class GiveUsFeedbackPage extends StatelessWidget {
  const GiveUsFeedbackPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Give Us Feedback Page'),
      ),
      body: Center(
        child: Text('Give Us Feedback Page'),
      ),
    );
  }
}
